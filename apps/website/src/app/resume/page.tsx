"use client";
import ResumePage from "#/features/resume/resume_page";
import React, { type ReactNode } from "react";

const ResumePageWrapper = (): ReactNode => {
    return <ResumePage />;
};

ResumePageWrapper.displayName = "ResumePageWrapper";

export default ResumePageWrapper;
