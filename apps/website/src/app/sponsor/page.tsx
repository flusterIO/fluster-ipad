import SponsorFlusterPage from "#/features/sponsor/sponsor_page/main";
import React, { type ReactNode } from "react";

const SponsorPage = (): ReactNode => {
    return <SponsorFlusterPage />;
};

SponsorPage.displayName = "SponsorPage";

export default SponsorPage;
