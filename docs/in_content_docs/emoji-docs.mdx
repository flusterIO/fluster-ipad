## Emojis <Emoji name="smile" smedium inline />

```long-docs
Don't forget that the `Emoji` _component_ is available alongside the built-in `:my_emoji:` syntax. This component accepts a **Sizable** option (see the `Sizable??` docs) as an optional boolean to allow you to use the emoji on the scale of an image, and all of the standard `Sizable` properties. You should probably prefer the boolean Sizable properties for media based components like images, svg's, videos and the like as they can be specifically hard to style without a complete set of properties that are designed to work together. Those boolean properties do just that, basically applying a few other properties in a way that's hopefully going to make sense.
```


<AutoInsertedNestedEmojiDocumentation />
