import { CodeEditor, WebViewContainer } from "@fluster/webview_utils";
import React from "react";

function App() {
    return (
        <WebViewContainer>
            <CodeEditor />
        </WebViewContainer>
    );
}

export default App;
