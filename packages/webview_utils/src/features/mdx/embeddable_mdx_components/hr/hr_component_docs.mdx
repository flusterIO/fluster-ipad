## Horizontal Rule (Hr)

````long-docs

The `Hr` component is almost identical to the standard horizontal rule in markdown, apart from that it accepts a `content` property that's a string

````

### Properties 

<<DOCUMENTATION_CONSTANT:docs/generated/zod/hr_properties.mdx>>
