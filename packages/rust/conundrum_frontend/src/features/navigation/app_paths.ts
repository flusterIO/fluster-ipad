export enum AppPaths {
    dashboard = "/",
    settings = "/settings",
    workspaces = "/workspaces",
    flashcards = "/flashcards",
    flashcardsQuiz = "/flashcardsQuiz",
    database = "/database",
}
