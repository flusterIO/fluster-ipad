import React, { type ReactNode } from "react";

interface ToolCallItemProps {
    item: object;
}

export const ToolCallItem = (props: ToolCallItemProps): ReactNode => {
    return <div></div>;
};

ToolCallItem.displayName = "ToolCallItem";
