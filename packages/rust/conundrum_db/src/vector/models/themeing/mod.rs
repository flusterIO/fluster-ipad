use arrow_schema::Field;

/// Todo: Get the fields from the theme in the conundrum class so it can be
/// saved in the DB to allow for multiple retrievable themes, and shared
/// themeing throughout the ecosystem.
pub fn get_theme_fields() -> Vec<Field> {
    todo!()
}
